<h1>Show a sinllge list</h1>

<h3>Add a bookmark to this list</h3>
<h1><%= @list.name %></h1>

<% @list.bookmarks.each do |b| %>
<%= b.comment %>
<%= b.movie_id %>
<%= b.list_id %>
<%= link_to "Delete this bookmark", bookmark_path(b), method: :delete %>
<br>
<% end %>

<%# <%= @list.movies[0].title %>

<% @list.movies.each do |m| %>
<%= m.title %>
<%= image_tag m.poster_url, height: 200 %>
<% end %>

<h1><%= link_to "Delete this list", list_path(@list), method: :delete %></h1>

<%# <%= cl_image_tag @list.photo.key, height: 300, width: 400, crop: :fill %>



<%# <%= @list.movies %>
<%# gives me the active record %>
<%# i have access to the movie id %>


<%= link_to "Add a bookmark", new_list_bookmark_path(@list) %>
<%= link_to "Home", root_path %>